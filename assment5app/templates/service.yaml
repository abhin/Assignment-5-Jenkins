apiVersion: v1
kind: Service
metadata:
  name: {{ include "assment5app.fullname" . }}
  labels:
    {{- include "assment5app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "assment5app.selectorLabels" . | nindent 4 }}
